name: project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0-315.0.dev

dependencies:
  flutter:
    sdk: flutter
  firebase_core:
  cloud_firestore: 
  intl:
  awesome_dialog:
  fluttertoast: ^8.0.9
  firebase_auth:
  hive_flutter:
  hive:
  go_router:
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: 
  hive_generator: 

flutter:
  uses-material-design: true
